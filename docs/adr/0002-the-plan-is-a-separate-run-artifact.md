# The Plan is a separate run artifact

Bottega Dex keeps the behavioral specification or verbatim request separate from a Plan that fixes ownership, builder non-decisions, named test interfaces, threat models, and vertical slices before builders start. The repository's documented planning authority owns the Plan when one exists; otherwise it remains in isolated run state or the conversation, because publishing it must not implicitly create or mutate a tracker item.
