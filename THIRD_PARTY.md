# Third-party material

## Autoreview review engine

- Upstream: `openclaw/agent-skills`, under `skills/autoreview`.
- Pinned at `fe588b1` (synced 2026-07-31; previously `98122a3`). `scripts/autoreview_test.py` and the changed fixture files are byte-identical to that revision. `scripts/autoreview` and `tests/test_autoreview_hardening.py` start from that revision and carry the local context-line security adaptation below. This sync adds the TruffleHog secret preflight and deletion-side redaction.
- Local adaptations: `plugins/bottega-dex/skills/code-review/SKILL.md` retains Bottega Dex's Codex Sol-to-Terra fallback, fixed GPT/Claude panel, Claude review defaults, model-run rules, and Dex-specific paths. The P2 threshold, selected-model smoke checks, and separate Standards and Spec reviews are present in the pinned Bottega `7ee58ba7ca3c0677c2f1405bdb52b4ba3b7a09b7` source plus the Codex-native Dex adaptation; they are not part of the unchanged OpenClaw `fe588b1` vendor pin. The OpenClaw advancement supplies the TruffleHog/deletion-redaction contract. `plugins/bottega-dex/skills/code-review/scripts/autoreview` adds a narrow fail-closed scan for unchanged unified-diff context lines, which are not present in the TruffleHog snapshot additions but are still sent to a review engine. `plugins/bottega-dex/skills/setup/SKILL.md` verifies `trufflehog` as a required review dependency and never installs it automatically.
- Sync: replace the listed vendor artifacts and changed fixtures as whole files, reconcile only the portable upstream contract hunks into the Dex skill, retain the Dex-specific review rules, and reapply the context-line security check with its regression test.

## Bottega workflow procedures

- Upstream: `vadimcomanescu/bottega`, under `skills/`.
- Pinned at `7ee58ba7ca3c0677c2f1405bdb52b4ba3b7a09b7` (Bottega 0.177.0). Dex adapts the surrounding procedures for Codex-native orchestration; `orchestrate` is separately pinned byte-for-byte in `README.md`.
- Local adaptations: discovery updates the repository glossary and qualifying ADRs inline; `bro` is a one-shot restatement and does not persist across turns, while Maestro owns its plain-language rule; multi-builder runs create one distinct slice branch and worktree per independent slice from the frozen integration base, each builder edits only its slice worktree, and only an accepted slice commit is integrated into the main run worktree; one review worker owns review convergence; the GPT-5.6 Sol and Claude Opus 5 panel is mandatory and fixed across reruns; the P2 smoke gate and separate Standards and Spec reviews apply; architecture remains a conditional independent read. Dex adopts Bottega's complete host-defined landing procedure, including its project-defined brake, procedure-assigned opener action, and procedure-read outcome, while retaining queue-owned non-draft semantics and the fail-closed opener-armed fallback.
