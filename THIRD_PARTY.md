# Third-party material

## Autoreview review engine

- Upstream: `openclaw/agent-skills`, under `skills/autoreview`.
- Pinned at `a504fd8ef2e704d95312d1c6c8afdf7e4466bcfc` (synced 2026-08-02; previously `fe588b1`). Both smoke-harness implementations, the shell smoke wrapper, and the vendored license are byte-identical to that revision. `scripts/autoreview`, `scripts/autoreview_test.py`, and `tests/test_autoreview_hardening.py` start from that revision and carry the local adaptations below. This sync adds isolated Kimi CLI support, unusable Java launcher handling in the vendored hardening suite, and the upstream secret and redaction hardening regressions.
- Local adaptations: `plugins/bottega-dex/skills/code-review/SKILL.md` retains Bottega Dex's public `code-review` name and paths, Codex Sol-to-Terra fallback, fixed integrated GPT/Claude panel, P2 threshold, exact selected-model smoke checks, separate Standards and Spec reviews, and Dex-specific run rules. Kimi remains a standalone helper capability and does not change Maestro's integrated panel. The P2 threshold, selected-model smoke checks, separate Standards and Spec reviews, finding verification check, authorized invariant and owner scope governor, and root-cause repair contract are present in the pinned Bottega `e0926de03d955febee9646d9c19d6384ec92a345` source plus the Codex-native Dex adaptation; they are not part of the unchanged OpenClaw vendor pin. The OpenClaw advancement supplies the review runtime and hardening tests. `plugins/bottega-dex/skills/code-review/scripts/autoreview` adds a narrow fail-closed scan for unchanged unified-diff context lines, which are not present in the TruffleHog snapshot additions but are still sent to a review engine. It also restricts Kimi to the official Kimi Code 0.x dialect, enables the 0.30 print-mode v2 contract, preserves validated managed OAuth endpoints, requires a complete `KIMI_MODEL_*` overlay instead of advertising ignored ambient direct-provider variables, validates the serialized Windows command line, and builds a complete prompt partition per reviewer so Kimi's argument ceiling does not reduce another panel member's context. `plugins/bottega-dex/skills/setup/SKILL.md` verifies `trufflehog` as a required review dependency and never installs it automatically.
- Sync: replace the listed vendor artifacts and changed fixtures as whole files, reconcile only the portable upstream contract hunks into the Dex skill, retain the Dex-specific review rules, and reapply the context-line security check with its regression test.

## Bottega workflow procedures

- Upstream: `vadimcomanescu/bottega`, under `skills/`.
- Pinned at `e0926de03d955febee9646d9c19d6384ec92a345` (Bottega 0.180.0). Dex adapts the surrounding procedures for Codex-native orchestration; `orchestrate` is separately pinned byte-for-byte in `README.md`.
- Local adaptations: discovery updates the repository glossary and qualifying ADRs inline; `bro` is a one-shot restatement and does not persist across turns, while Maestro owns its plain-language rule; multi-builder runs create one distinct slice branch and worktree per independent slice from the frozen integration base, each builder edits only its slice worktree, and only an accepted slice commit is integrated into the main run worktree; one review worker owns review convergence; the GPT-5.6 Sol and Claude Opus 5 panel is mandatory and fixed across reruns; the P2 smoke gate and separate Standards and Spec reviews apply; architecture remains a conditional independent read. Dex adopts Bottega's complete host-defined landing procedure, including its project-defined brake, procedure-assigned opener action, and procedure-read outcome, while retaining queue-owned non-draft semantics and the fail-closed opener-armed fallback.
