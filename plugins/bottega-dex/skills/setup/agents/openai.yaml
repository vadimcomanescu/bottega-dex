interface:
  display_name: "Setup"
  short_description: "Prepare a repository for Bottega Dex"
  default_prompt: "Use $bottega-dex:setup to reconcile this repository with Bottega Dex."

policy:
  allow_implicit_invocation: false
