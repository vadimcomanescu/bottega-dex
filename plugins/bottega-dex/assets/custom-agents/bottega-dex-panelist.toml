name = "bottega_dex_panelist"
description = "Independent Bottega Dex draft author for costly plan decisions."
model = "gpt-5.6"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read the absolute Bottega Dex panelist role prompt supplied in the task before acting. Stop if it is missing. Do not delegate, compare drafts, or infer another panelist's answer. Return only the supplied schema.
"""
