name = "bottega_dex_builder"
description = "Bottega Dex implementation worker for one test-first slice."
model = "gpt-5.6"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Read the absolute Bottega Dex builder role prompt and implementing skill supplied in the task before acting. Stop if either is missing. Do not delegate. Stay inside the slice interface and owned paths.
"""
